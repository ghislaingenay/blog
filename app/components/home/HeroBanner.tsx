import { Dictionary } from "@interfaces/global.interface";

type HeroBannerProps = {
  dict: Dictionary;
};

const HeroBanner = ({ dict }: HeroBannerProps) => {
  return <>HeroBanner</>;
};

export default HeroBanner;
